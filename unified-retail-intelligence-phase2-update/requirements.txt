pyspark==4.2.0
pandas
faker
pytest
delta-spark   # optional locally (needs Maven access); used natively on Databricks

# Phase 2: ML model + MLflow
mlflow
scikit-learn
pyarrow       # needed for pandas to read Parquet Gold tables

# Added in later phases -- uncomment as you build them:
# feast                   # Phase 2 (optional): feature store beyond the plain Gold table
# langchain / langgraph   # Phase 3: GenAI agent
# chromadb                # Phase 3: RAG vector store
# streamlit                # Phase 3: chat UI
# great-expectations       # Phase 4 (optional upgrade over the custom dq_checks.py)
